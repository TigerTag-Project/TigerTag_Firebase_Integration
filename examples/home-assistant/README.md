# Home Assistant custom component (skeleton)

Place under `<config>/custom_components/tigertag/` then add via Settings →
Devices & Services → Add Integration → "TigerTag".

Skeleton code is in [docs/clients/home-assistant.md § 9](../../docs/clients/home-assistant.md#9-home-assistant-integration-sketch).

A fully runnable component is TODO — contributions welcome.
