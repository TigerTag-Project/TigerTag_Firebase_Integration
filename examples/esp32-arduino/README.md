# ESP32 / Arduino example

For a working sketch (auth + RFID scan + weight write), see the TigerScale
firmware repository.

This folder is a placeholder for a minimal "sign in + read inventory"
example — TODO.
